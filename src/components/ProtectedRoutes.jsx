/**
 *  If not logged in only allow user to access the welcome page untile they either log in or create a account
 */

const useAuth = () => {
    // authentication happens here
    // return true or false based on authentication status
}

const ProtectedRoutes = () => {

}